import { css } from "@emotion/react";

import { MAX_WIDTH } from "@constants/width";

export const searchPageOuterStyle = css`
  margin: 20px 0;
  max-width: ${MAX_WIDTH.lg}px;
`;

export const searchPageHeaderStyle = css`
  height: 48px;
  gap: 10px;
`;
